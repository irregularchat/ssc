{
  "name": "ssc",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "deploy": "./deploy.sh",
    "deploy:milnav": "./deploy.sh milnav",
    "deploy:packing-list": "./deploy.sh packing-list",
    "deploy:all": "./deploy.sh all"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
