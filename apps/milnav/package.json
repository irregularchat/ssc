{
  "name": "milnav",
  "version": "1.0.0",
  "private": true,
  "description": "Military installation building number navigator",
  "main": "api/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'milnav api: no build step (wrangler bundles)'",
    "deploy": "./deploy.sh",
    "deploy:api": "./deploy.sh --api",
    "deploy:web": "./deploy.sh --web"
  },
  "dependencies": {
    "@ssc/cloudflare-utils": "workspace:*",
    "hono": "^4.11.3",
    "mgrs": "^2.1.0",
    "open-location-code": "^1.0.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251230.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  }
}
